# JWT Configuration
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters

# Database Configuration
# Local Docker (port 5433 to avoid conflicts):
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/takeweb"

# Public ERPNext/Frappe integration used by the NestJS careers module.
# No ERP credentials are required for the guest-enabled website methods.
ERP_BASE_URL=https://admin.takeweb.in

# Production (Neon example):
# DATABASE_URL="postgresql://user:pass@xxx.neon.tech/takeweb?sslmode=require"

# Optional: JWT Expiration (default: 7d)
# JWT_EXPIRES_IN=7d

# Optional: CORS Origins (for production)
# FRONTEND_URL=https://takeweb.vercel.app
# ADMIN_URL=https://takeweb-admin.vercel.app
